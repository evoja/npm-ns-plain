{
  "name": "@evoja/ns-plain",
  "version": "0.1.0-s1",
  "license": "MIT",
  "repository": "git@github.com:evoja/npm-ns-plain.git",

  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": ["lib"],

  "scripts": {
    "mypublish": "npm test && npm publish --access public",

    "clean": "rm -rf lib && rm -rf out",
    "build": "tsc -p src --outDir lib",
    "build-tests": "tsc -p test --outDir out",
    "watch": "tsc -p test --watch --outDir out",
    "test": "npm run clean && npm run build-tests && npm run just-test",
    "just-test": "jest out/test/tests --config=jest.conf.json"
  },

  "//devDependencies": {
    "//": "Testing purposes",
    "jest": "Test runner"
  },

  "devDependencies": {
    "typescript": "3.7.3",
    "jest" : "25.1.0",
    "@types/jest" : "25.1.1"
  }
}
